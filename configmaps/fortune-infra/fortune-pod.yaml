apiVersion: v1
kind: Pod
metadata:
  labels:
    app: fortune
  name: fortune-pod
spec:
  volumes:
    - name: html
      emptyDir: {}
  containers:
  - image: nginx
    name: web-server
    ports:
      - containerPort: 80
        protocol: TCP
    volumeMounts:
      - name: html                           
        mountPath: /usr/share/nginx/html     
        readOnly: true
  - image: anandaili/fortune:env
    name: html-generator
    env:
      - name: INTERVAL
        value: "30"
    volumeMounts:
      - name: html 
        mountPath: /var/htdocs 
       
    
